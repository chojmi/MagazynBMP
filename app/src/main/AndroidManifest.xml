<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="michalchojnacki.magazynbmp"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application

        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/AppName"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".controllers.resControllers.activities.StartActivity"
            android:label="@string/AppName">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".controllers.resControllers.activities.SparePartViewer"/>
        <activity android:name=".controllers.resControllers.activities.SparePartsViewer"/>
        <activity android:name=".controllers.resControllers.activities.SparePartsBasketViewer"/>
        <activity
            android:name="com.nononsenseapps.filepicker.FilePickerActivity"
            android:label="@string/AppName"
            android:theme="@style/FilePickerTheme">
            <intent-filter>
                <action android:name="android.intent.action.GET_CONTENT"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
    </application>

</manifest>
